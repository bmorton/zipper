name: 'Setup Go'
description: 'Setup Go environment'
runs:
  using: 'composite'
  steps:
    - name: Set up Go
      uses: actions/setup-go@v6
      with:
        go-version: '1.26.x'
        cache: true

    - name: Download all module dependencies
      run: go mod download
      shell: bash
